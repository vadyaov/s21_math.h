#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <limits.h>
#include "s21_math.h"

int main() {
    // printf("%.10Lf\n", s21_exp(5));
    printf("%lf", (int)(5.55555 * 10) % 10);
    // long double e = 0.0;
    // for (int i = 0; i < 34; i++) {
    //     e += (1.0 / s21_factorial(i));
    //     printf("%.15Lf\n", e);
    // }
    return 0;
}
